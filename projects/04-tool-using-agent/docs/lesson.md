# Lesson

Tools turn an LLM from a text generator into an agent that can act. The model requests a tool; the runtime validates and executes it.

## What changes in this step

- Provider responses can include tool calls.
- The agent runtime owns execution.
- Tool results are fed back into the conversation.

## Key idea

The model proposes actions; the agent runtime decides how to validate and execute them.


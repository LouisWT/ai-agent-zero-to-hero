# Lesson

Shell access makes an agent useful for development tasks, but it is one of the highest-risk tools. This step adds a constrained shell rather than a general terminal.

## What changes in this step

- The agent can execute commands.
- Output is structured for the model.
- Risky commands are blocked before execution.

## Key idea

The shell tool should be useful by default and dangerous only with explicit future approval mechanisms.

